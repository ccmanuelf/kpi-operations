/**
 * Alerts API Module
 *
 * NOTE: All 15 functions removed (Run 3, Sprint 6, DC-05).
 * AlertDashboard.vue uses direct api.get()/api.post() calls.
 * WorkflowStepEquipment.vue defines its own local acknowledgeAlert().
 * Re-add functions here if alert API abstraction is needed in the future.
 */
