/**
 * Ambient declaration for the still-JS capacity analysis sub-store
 * (MRP, capacity analysis, schedules, scenarios, KPI actuals).
 * Drop this when useAnalysisStore.js is ported to .ts.
 */
export const useAnalysisStore: any
