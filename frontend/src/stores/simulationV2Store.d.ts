/**
 * Ambient declaration for simulationV2Store.js so TS-side imports
 * (relative or @-aliased) resolve without TS7016. The store is still
 * implemented in JS; when it gets ported to .ts, delete this file.
 */
export const useSimulationV2Store: any
