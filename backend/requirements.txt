# FastAPI Backend Requirements
# Python 3.11+

# Core Framework
fastapi==0.138.1
starlette==1.3.1       # PYSEC-2026-161 fix (transitive via fastapi; was 0.52.1)
uvicorn[standard]==0.49.0
pydantic[email]==2.13.4
pydantic-settings==2.14.2  # GHSA-4xgf-cpjx-pc3j (disclosed 2026-06-21); 2.14.1 vulnerable

# Database
sqlalchemy==2.0.51
alembic==1.18.5
pymysql==1.1.2
cryptography==48.0.1  # GHSA-537c-gmf6-5ccf + prior CVEs; pinned for pymysql SSL; security-critical

# Authentication
pyjwt==2.13.0          # HS256 JWTs (replaced unmaintained python-jose, Run 7); HMAC needs no crypto extra
# python-jose removed (Run 7): dropped with its compensating CVE pins ecdsa /
# pyasn1 / rsa, which only existed for jose's unused EC/RSA paths.
argon2-cffi==25.1.0    # Primary password-hashing (OWASP-recommended algorithm)
bcrypt==5.0.0          # Retained for legacy-hash verification path (rehash-on-verify migration shim from passlib era)
python-multipart==0.0.31  # GHSA-v9pg-7xvm-68hf + GHSA-5rvq-cxj2-64vf + GHSA-6jv3-5f52-599m + prior CVEs

# CORS & Security
python-dotenv==1.2.2   # CVE-2026-28684 fix
slowapi==0.1.9

# Date Utilities
python-dateutil==2.9.0.post0

# CSV / Excel Processing
openpyxl==3.1.5

# PDF Generation
reportlab==4.4.10
pillow==12.2.0          # transitive via reportlab; pinned for 5 CVEs (40192, 42308–42311)

# Email & Reports
sendgrid==6.12.5
werkzeug==3.1.8         # transitive via sendgrid; pinned for CVE-2026-27199
APScheduler==3.11.3

# System Monitoring
psutil==7.2.2

# QR Code Generation
qrcode[pil]==8.2
simpy==4.1.2
