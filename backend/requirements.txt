# FastAPI Backend Requirements
# Python 3.11+

# Core Framework
fastapi==0.141.1
starlette==1.3.1       # PYSEC-2026-161 fix (transitive via fastapi; was 0.52.1)
uvicorn[standard]==0.52.4
pydantic[email]==2.13.5
pydantic-settings==2.15.0  # GHSA-4xgf-cpjx-pc3j (disclosed 2026-06-21); 2.14.1 vulnerable

# Database
sqlalchemy==2.0.52
alembic==1.19.1
pymysql==1.2.0
cryptography==50.0.1  # GHSA-m2h6-j472-rp4c/GHSA-g6cj-pr64-35w5/GHSA-jwv3-5hgf-82ww (2026-08) + prior CVEs; pinned for pymysql SSL; security-critical

# Authentication
pyjwt==2.13.0          # HS256 JWTs (replaced unmaintained python-jose, Run 7); HMAC needs no crypto extra
# python-jose removed (Run 7): dropped with its compensating CVE pins ecdsa /
# pyasn1 / rsa, which only existed for jose's unused EC/RSA paths.
argon2-cffi==25.1.0    # Primary password-hashing (OWASP-recommended algorithm)
bcrypt==5.0.0          # Retained for legacy-hash verification path (rehash-on-verify migration shim from passlib era)
python-multipart==0.0.32  # GHSA-v9pg-7xvm-68hf + GHSA-5rvq-cxj2-64vf + GHSA-6jv3-5f52-599m + prior CVEs

# CORS & Security
python-dotenv==1.2.3   # CVE-2026-28684 fix
slowapi==0.1.10

# Date Utilities
python-dateutil==2.9.0.post0

# CSV / Excel Processing
openpyxl==3.1.5

# PDF Generation
reportlab==5.0.1
pillow==12.3.0          # transitive via reportlab; pinned for CVEs (40192, 42308–42311; PYSEC-2026-2253–2257 fixed in 12.3.0)

# Email & Reports
sendgrid==6.12.5
werkzeug==3.1.8         # transitive via sendgrid; pinned for CVE-2026-27199
APScheduler==3.11.3

# System Monitoring
psutil==7.2.2

# QR Code Generation
qrcode[pil]==8.2
simpy==4.1.2

# Production WSGI/ASGI process manager (VM compose stack; Render stays on plain uvicorn)
gunicorn==26.2.0
# The gunicorn worker class, split out of uvicorn itself. uvicorn 0.30
# deprecated `uvicorn.workers` (it still ships in 0.52 but warns on import,
# pointing here); the VM's docker-compose.prod.yml command uses
# `-k uvicorn_worker.UvicornWorker`. Only the VM stack runs gunicorn --
# Render uses the Dockerfile's plain-uvicorn CMD.
uvicorn-worker==0.4.0
