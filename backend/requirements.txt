# FastAPI Backend Requirements
# Python 3.11+

# Core Framework
fastapi==0.129.0
uvicorn[standard]==0.48.0
pydantic[email]==2.13.4
pydantic-settings==2.14.1

# Database
sqlalchemy==2.0.46
alembic==1.15.2
pymysql==1.1.2
cryptography==46.0.7  # CVE-2026-34073 + CVE-2026-39892 fix; pinned for pymysql SSL + python-jose; security-critical

# Authentication
python-jose[cryptography]==3.5.0
ecdsa==0.19.2          # transitive via python-jose; pinned for CVE-2024-23342 + CVE-2026-33936
pyasn1==0.6.3          # transitive via python-jose + rsa; pinned for CVE-2026-30922
argon2-cffi==25.1.0    # Primary password-hashing (OWASP-recommended algorithm)
bcrypt==5.0.0          # Retained for legacy-hash verification path (rehash-on-verify migration shim from passlib era)
python-multipart==0.0.27  # CVE-2026-40347 + CVE-2026-42561 fix

# CORS & Security
python-dotenv==1.2.2   # CVE-2026-28684 fix
slowapi==0.1.9

# Date Utilities
python-dateutil==2.9.0.post0

# CSV / Excel Processing
openpyxl==3.1.5

# PDF Generation
reportlab==4.4.10
pillow==12.2.0          # transitive via reportlab; pinned for 5 CVEs (40192, 42308–42311)

# Email & Reports
sendgrid==6.12.5
werkzeug==3.1.8         # transitive via sendgrid; pinned for CVE-2026-27199
APScheduler==3.11.2

# System Monitoring
psutil==7.2.2

# QR Code Generation
qrcode[pil]==8.2
simpy==4.1.1
