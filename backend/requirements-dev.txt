# Dev / Test Dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==9.1.1          # CVE-2025-71176 fix (was 8.3.5)
pytest-asyncio==1.4.0  # pytest 9 compat (was 0.25.3)
pytest-cov==7.1.0      # pytest 9 compat (was 6.1.1)
httpx==0.28.1

# Code Quality
bandit==1.9.4
black==26.5.1          # CVE-2026-32274 fix (was 25.1.0)
flake8==7.3.0
mypy==2.3.1

# Security gates (Run 7 T2.1/T2.2) — CI fails on new secrets / known CVEs
detect-secrets==1.5.0
pip-audit==2.10.1

# Type stubs (required by mypy --no-error-summary)
types-python-dateutil==2.9.0.20260807
# PyYAML ships no stubs of its own, and backend/tests/test_ci_workflow_gates.py
# parses ci.yml with it. Without these, `import yaml` is Any under mypy 2.x and
# an outright import-untyped error under mypy 1.x -- so the correct spelling of
# that import depended on which mypy you happened to have, and the two versions
# disagreed (CI runs the locked 2.1.0; a stale ambient 1.17.1 wanted the
# opposite). Installing the stubs makes one spelling right everywhere AND type-
# checks the yaml calls instead of treating them as Any.
types-PyYAML==6.0.12.20260815

# Lock tooling (C4) — compiles the hash-pinned *.lock files
pip-tools==7.6.1
